class ReferralStep {
  final int step;
  final String title;
  final String description;

  const ReferralStep({
    required this.step,
    required this.title,
    required this.description,
  });
}
