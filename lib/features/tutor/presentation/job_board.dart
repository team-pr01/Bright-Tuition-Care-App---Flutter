import 'package:flutter/material.dart';

class TutorJobsScreen extends StatelessWidget {
  const TutorJobsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tutor Jobs"),
    );
  }
}
   